# Copy to application-local.yml and set your local MySQL credentials.
# application-local.yml is gitignored — never commit passwords.

spring:
  datasource:
    username: root
    password: your-mysql-password-here
